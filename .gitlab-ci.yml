stages: 
 - build

build & deploy:
  image: docker:latest
  stage: build
  services:
    - docker:dind
    
  before_script:
    - echo $DOCKER_PASSWORD | docker login --username $DOCKER_USERNAME --password-stdin

  script:
    - docker build -t simple-flask .  
    - docker push awakz/simple-flask:latest
